import React from "react";

/**
 * @description
 *  Context for BookService instance
 */
const BookServiceContext = React.createContext({});
BookServiceContext.displayName = "BookService context";

export default BookServiceContext;
